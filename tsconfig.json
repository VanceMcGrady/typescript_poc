{
  "compilerOptions": {
    "target": "es2016",                                                                  /* Specify what module code is generated. */                /* Allow accessing UMD globals from modules. */                   /* Specify the maximum folder depth used for checking JavaScript files from 'node_modules'. Only applicable with 'allowJs'.                                   /* Disable emitting files from a compilation. */
    "moduleResolution": "node",
    "outDir": "dist",
    "module": "commonjs",  
  },
  "include": ["src"] 
}
